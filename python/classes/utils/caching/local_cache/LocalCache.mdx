---
title: 'LocalCache'
---

```python
class LocalCache
```

<p align="right">[source code](https://github.com/unifyai/unify/tree/b6603377dfc64cb29a1e4a7c1414c501c680598e/unify/utils/caching/local_cache.py#L18)</p>

Local file-based cache implementation.

## setters

---

### set\_cache\_name

<p align="right">[source code](https://github.com/unifyai/unify/tree/b6603377dfc64cb29a1e4a7c1414c501c680598e/unify/utils/caching/local_cache.py#L27)</p>

```python
def set_cache_name(cls, name: str) -> None:
```

Set the cache filename and reset the in-memory cache.

## methods

---

### get\_cache\_filepath

<p align="right">[source code](https://github.com/unifyai/unify/tree/b6603377dfc64cb29a1e4a7c1414c501c680598e/unify/utils/caching/local_cache.py#L38)</p>

```python
def get_cache_filepath(cls, name: str = None) -> str:
```

Get the full filepath for the cache file.

---

### get\_cache\_name

<p align="right">[source code](https://github.com/unifyai/unify/tree/b6603377dfc64cb29a1e4a7c1414c501c680598e/unify/utils/caching/local_cache.py#L33)</p>

```python
def get_cache_name(cls) -> str:
```

Get the current cache filename.

---

### has\_key

<p align="right">[source code](https://github.com/unifyai/unify/tree/b6603377dfc64cb29a1e4a7c1414c501c680598e/unify/utils/caching/local_cache.py#L107)</p>

```python
def has_key(cls, key: str) -> bool:
```

Check if a key exists in the cache.

---

### initialize\_cache

<p align="right">[source code](https://github.com/unifyai/unify/tree/b6603377dfc64cb29a1e4a7c1414c501c680598e/unify/utils/caching/local_cache.py#L63)</p>

```python
def initialize_cache(cls, name: str = None) -> None:
```

Initialize or load the cache from disk.

---

### is\_enabled

<p align="right">[source code](https://github.com/unifyai/unify/tree/b6603377dfc64cb29a1e4a7c1414c501c680598e/unify/utils/caching/local_cache.py#L45)</p>

```python
def is_enabled(cls) -> bool:
```

Check if the cache is enabled.

---

### list\_keys

<p align="right">[source code](https://github.com/unifyai/unify/tree/b6603377dfc64cb29a1e4a7c1414c501c680598e/unify/utils/caching/local_cache.py#L85)</p>

```python
def list_keys(cls) -> List[str]:
```



---

### remove\_entry

<p align="right">[source code](https://github.com/unifyai/unify/tree/b6603377dfc64cb29a1e4a7c1414c501c680598e/unify/utils/caching/local_cache.py#L112)</p>

```python
def remove_entry(cls, key: str) -> None:
```

Remove an entry and its res_types from the cache.

---

### retrieve\_entry

<p align="right">[source code](https://github.com/unifyai/unify/tree/b6603377dfc64cb29a1e4a7c1414c501c680598e/unify/utils/caching/local_cache.py#L89)</p>

```python
def retrieve_entry(cls, key: str) -> tuple[Optional[Any], Optional[Dict[str, Any]]]:
```

Retrieve a value from the cache.

**Returns**:

Tuple of (value, type_registry) or (None, None) if not found

---

### serialize\_object

<p align="right">[source code](https://github.com/unifyai/unify/tree/b6603377dfc64cb29a1e4a7c1414c501c680598e/unify/utils/caching/base_cache.py#L18)</p>

```python
def serialize_object(
        obj: Any,
        cached_types: Dict[str, str] = None,
        idx: List[Union[str, int]] = None,
        indent: int = None,
    ) -> Any:
```

Serialize an object to a JSON-serializable format.

**Arguments**:

- `obj` - Object to serialize
- `type_registry` - Dictionary to track object types for reconstruction
- `path` - Current path in the object structure
- `indent` - JSON indentation level



**Returns**:

Serialized object or JSON string if at root level

---

### store\_entry

<p align="right">[source code](https://github.com/unifyai/unify/tree/b6603377dfc64cb29a1e4a7c1414c501c680598e/unify/utils/caching/local_cache.py#L50)</p>

```python
def store_entry(
        cls,
        *,
        key: str,
        value: Any,
        res_types: Optional[Dict[str, Any]] = None,
    ) -> None:
```

Store a key-value pair in the cache.