---
title: 'BaseCache'
---

```python
class BaseCache
```

<p align="right">[source code](https://github.com/unifyai/unify/tree/11db1d5adf066d1f2ecdf07f258efb0f4d0ce3ef/unify/utils/caching/base_cache.py#L15)</p>

Abstract base class for cache implementations.

## setters

---

### set\_cache\_name

<p align="right">[source code](https://github.com/unifyai/unify/tree/11db1d5adf066d1f2ecdf07f258efb0f4d0ce3ef/unify/utils/caching/base_cache.py#L73)</p>

```python
def set_cache_name(cls, name: str) -> None:
```

Set the cache identifier/name.

## methods

---

### get\_cache\_name

<p align="right">[source code](https://github.com/unifyai/unify/tree/11db1d5adf066d1f2ecdf07f258efb0f4d0ce3ef/unify/utils/caching/base_cache.py#L78)</p>

```python
def get_cache_name(cls) -> str:
```

Get the current cache identifier/name.

---

### has\_key

<p align="right">[source code](https://github.com/unifyai/unify/tree/11db1d5adf066d1f2ecdf07f258efb0f4d0ce3ef/unify/utils/caching/base_cache.py#L114)</p>

```python
def has_key(cls, key: str) -> bool:
```

Check if a key exists in the cache.

---

### initialize\_cache

<p align="right">[source code](https://github.com/unifyai/unify/tree/11db1d5adf066d1f2ecdf07f258efb0f4d0ce3ef/unify/utils/caching/base_cache.py#L94)</p>

```python
def initialize_cache(cls, name: str = None) -> None:
```

Initialize or load the cache from storage.

---

### list\_keys

<p align="right">[source code](https://github.com/unifyai/unify/tree/11db1d5adf066d1f2ecdf07f258efb0f4d0ce3ef/unify/utils/caching/base_cache.py#L99)</p>

```python
def list_keys(cls) -> List[str]:
```

Get a list of all cache keys.

---

### remove\_entry

<p align="right">[source code](https://github.com/unifyai/unify/tree/11db1d5adf066d1f2ecdf07f258efb0f4d0ce3ef/unify/utils/caching/base_cache.py#L119)</p>

```python
def remove_entry(cls, key: str) -> None:
```

Remove an entry from the cache.

---

### retrieve\_entry

<p align="right">[source code](https://github.com/unifyai/unify/tree/11db1d5adf066d1f2ecdf07f258efb0f4d0ce3ef/unify/utils/caching/base_cache.py#L104)</p>

```python
def retrieve_entry(cls, key: str) -> tuple[Optional[Any], Optional[Dict[str, Any]]]:
```

Retrieve a value from the cache.

**Returns**:

Tuple of (value, res_types) or (None, None) if not found

---

### serialize\_object

<p align="right">[source code](https://github.com/unifyai/unify/tree/11db1d5adf066d1f2ecdf07f258efb0f4d0ce3ef/unify/utils/caching/base_cache.py#L18)</p>

```python
def serialize_object(
        obj: Any,
        cached_types: Dict[str, str] = None,
        idx: List[Union[str, int]] = None,
        indent: int = None,
    ) -> Any:
```

Serialize an object to a JSON-serializable format.

**Arguments**:

- `obj` - Object to serialize
- `type_registry` - Dictionary to track object types for reconstruction
- `path` - Current path in the object structure
- `indent` - JSON indentation level



**Returns**:

Serialized object or JSON string if at root level

---

### store\_entry

<p align="right">[source code](https://github.com/unifyai/unify/tree/11db1d5adf066d1f2ecdf07f258efb0f4d0ce3ef/unify/utils/caching/base_cache.py#L83)</p>

```python
def store_entry(
        cls,
        *,
        key: str,
        value: Any,
        res_types: Optional[Dict[str, Any]] = None,
    ) -> None:
```

Store a key-value pair in the cache.