---
title: 'RemoteCache'
---

```python
class RemoteCache
```

<p align="right">[source code](https://github.com/unifyai/unify/tree/0e805755ab6f3021d9659281214eca2f50a71509/unify/utils/caching/remote_cache.py#L13)</p>

Remote cache implementation using the logging system.

## setters

---

### set\_cache\_name

<p align="right">[source code](https://github.com/unifyai/unify/tree/0e805755ab6f3021d9659281214eca2f50a71509/unify/utils/caching/remote_cache.py#L23)</p>

```python
def set_cache_name(cls, name: str) -> None:
```

Set the remote context name for the cache.

## methods

---

### get\_cache\_name

<p align="right">[source code](https://github.com/unifyai/unify/tree/0e805755ab6f3021d9659281214eca2f50a71509/unify/utils/caching/remote_cache.py#L28)</p>

```python
def get_cache_name(cls) -> str:
```

Get the current remote context name.

---

### has\_key

<p align="right">[source code](https://github.com/unifyai/unify/tree/0e805755ab6f3021d9659281214eca2f50a71509/unify/utils/caching/remote_cache.py#L107)</p>

```python
def has_key(cls, key: str) -> bool:
```

Check if a key exists in the remote cache.

---

### initialize\_cache

<p align="right">[source code](https://github.com/unifyai/unify/tree/0e805755ab6f3021d9659281214eca2f50a71509/unify/utils/caching/remote_cache.py#L64)</p>

```python
def initialize_cache(cls, name: str = None) -> None:
```

Ensure the remote context exists.

---

### list\_keys

<p align="right">[source code](https://github.com/unifyai/unify/tree/0e805755ab6f3021d9659281214eca2f50a71509/unify/utils/caching/remote_cache.py#L72)</p>

```python
def list_keys(cls) -> List[str]:
```

Get a list of all cache keys from the remote context.

---

### remove\_entry

<p align="right">[source code](https://github.com/unifyai/unify/tree/0e805755ab6f3021d9659281214eca2f50a71509/unify/utils/caching/remote_cache.py#L119)</p>

```python
def remove_entry(cls, key: str) -> None:
```

Remove an entry from the remote cache.

---

### retrieve\_entry

<p align="right">[source code](https://github.com/unifyai/unify/tree/0e805755ab6f3021d9659281214eca2f50a71509/unify/utils/caching/remote_cache.py#L80)</p>

```python
def retrieve_entry(cls, key: str) -> tuple[Optional[Any], Optional[Dict[str, Any]]]:
```

Retrieve a value from the remote cache.

**Returns**:

Tuple of (value, res_types) or (None, None) if not found

---

### serialize\_object

<p align="right">[source code](https://github.com/unifyai/unify/tree/0e805755ab6f3021d9659281214eca2f50a71509/unify/utils/caching/base_cache.py#L18)</p>

```python
def serialize_object(
        obj: Any,
        cached_types: Dict[str, str] = None,
        idx: List[Union[str, int]] = None,
        indent: int = None,
    ) -> Any:
```

Serialize an object to a JSON-serializable format.

**Arguments**:

- `obj` - Object to serialize
- `type_registry` - Dictionary to track object types for reconstruction
- `path` - Current path in the object structure
- `indent` - JSON indentation level



**Returns**:

Serialized object or JSON string if at root level

---

### store\_entry

<p align="right">[source code](https://github.com/unifyai/unify/tree/0e805755ab6f3021d9659281214eca2f50a71509/unify/utils/caching/remote_cache.py#L33)</p>

```python
def store_entry(
        cls,
        *,
        key: str,
        value: Any,
        res_types: Optional[Dict[str, Any]] = None,
    ) -> None:
```

Store a key-value pair in the remote cache.

---

### write

<p align="right">[source code](https://github.com/unifyai/unify/tree/0e805755ab6f3021d9659281214eca2f50a71509/unify/utils/caching/remote_cache.py#L60)</p>

```python
def write(cls, filename: str = None) -> None:
```

No-op for remote cache - data is persisted immediately.