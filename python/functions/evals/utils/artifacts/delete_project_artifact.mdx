---
title: 'delete_project_artifact'
---

```python
def delete_project_artifact(
    key: str,
    *,
    project: Optional[str] = None,
    api_key: Optional[str] = None,
) -> str:
```

<p align="right">[source code](https://github.com/unifyai/unify/tree/ec9b58f6a70878c2dc514d39fcd3a7501f6ea0ec/unify/evals/utils/artifacts.py#L50)</p>

Deletes an artifact from a project.

**Arguments**:

- `project` - Name of the project to delete an artifact from.
- `key` - Key of the artifact to delete.
- `api_key` - If specified, unify API key to be used. Defaults to the value in the



**Returns**:

Whether the artifact was successfully deleted.