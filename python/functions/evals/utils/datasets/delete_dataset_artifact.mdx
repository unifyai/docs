---
title: 'delete_dataset_artifact'
---

```python
def delete_dataset_artifact(
    name: str,
    key: str,
    *,
    api_key: Optional[str] = None,
) -> Union[List[Any], None]:
```

<p align="right">[source code](https://github.com/unifyai/unify/tree/1c9f60db96acc26174417302338be8ad964b89b8/unify/evals/utils/datasets.py#L368)</p>

Deletes dataset artifact from the platform.

**Arguments**:

- `name` - Name of the dataset to delete an artifact from.
- `key` - The key of the artifact to delete
- `api_key` - If specified, unify API key to be used. Defaults to the value in the