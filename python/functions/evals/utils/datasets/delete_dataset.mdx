---
title: 'delete_dataset'
---

```python
def delete_dataset(
    name: str,
    *,
    api_key: Optional[str] = None,
) -> str:
```

<p align="right">[source code](https://github.com/unifyai/unify/tree/cf1b32da132b79c501c68b649173f4a6d726a5d2/unify/evals/utils/datasets.py#L98)</p>

Deletes a dataset from the platform.

**Arguments**:

- `name` - Name given to the uploaded dataset.
- `api_key` - If specified, unify API key to be used. Defaults to the value in the



**Returns**:

- `str` - Info msg with the response from the HTTP endpoint.
- `ValueError` - If there was an HTTP error.