---
title: 'add_dataset_entries'
---

```python
def add_dataset_entries(
    name: str,
    data: List[Any],
    *,
    project: Optional[str] = None,
    api_key: Optional[str] = None,
) -> List[int]:
```

<p align="right">[source code](https://github.com/unifyai/unify/tree/e53f8cfbe91a8e4c6cfd24db0fed3e7b444ad3f6/unify/logging/utils/datasets.py#L212)</p>

Adds entries to an existing dataset in the server.

**Arguments**:

- `name` - Name of the dataset.
- `contents` - Contents to add to the dataset.
- `project` - Name of the project the dataset belongs to.
- `api_key` - If specified, unify API key to be used. Defaults to the value in the