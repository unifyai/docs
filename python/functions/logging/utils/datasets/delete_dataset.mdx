---
title: 'delete_dataset'
---

```python
def delete_dataset(
    name: str,
    *,
    project: Optional[str] = None,
    api_key: Optional[str] = None,
) -> None:
```

<p align="right">[source code](https://github.com/unifyai/unify/tree/20f7559ff0f134f5e9fbf9428151cf249a87a226/unify/logging/utils/datasets.py#L190)</p>

Delete a dataset from the server.

**Arguments**:

- `name` - Name of the dataset.
- `project` - Name of the project the dataset belongs to.
- `api_key` - If specified, unify API key to be used. Defaults to the value in the