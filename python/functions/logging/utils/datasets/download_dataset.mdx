---
title: 'download_dataset'
---

```python
def download_dataset(
    name: str,
    *,
    project: Optional[str] = None,
    api_key: Optional[str] = None,
) -> List[Log]:
```

<p align="right">[source code](https://github.com/unifyai/unify/tree/9ead467bc5c559d351b833afc6a3571c2ca8f506/unify/logging/utils/datasets.py#L165)</p>

Download a dataset from the server.

**Arguments**:

- `name` - Name of the dataset.
- `project` - Name of the project the dataset belongs to.
- `api_key` - If specified, unify API key to be used. Defaults to the value in the