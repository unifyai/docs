---
title: 'delete_context'
---

```python
def delete_context(
    name: str,
    *,
    project: Optional[str] = None,
    api_key: Optional[str] = None,
) -> None:
```

<p align="right">[source code](https://github.com/unifyai/unify/tree/cfb1289c27e82c6b7cc22d89c9ea6ff6e2818951/unify/logging/utils/contexts.py#L116)</p>

Delete a context from the server.

**Arguments**:

- `name` - Name of the context to delete.
- `project` - Name of the project the context belongs to.
- `api_key` - If specified, unify API key to be used. Defaults to the value in the