---
title: 'rename_context'
---

```python
def rename_context(
    name: str,
    new_name: str,
    *,
    project: Optional[str] = None,
    api_key: Optional[str] = None,
) -> None:
```

<p align="right">[source code](https://github.com/unifyai/unify/tree/3536df30cd6727763936e6b60fd9f3e27b1860a3/unify/logging/utils/contexts.py#L257)</p>

Rename a context.

**Arguments**:

- `name` - Name of the context to rename.
- `new_name` - New name of the context.
- `project` - Name of the project the context belongs to.
- `api_key` - If specified, unify API key to be used. Defaults to the value in the