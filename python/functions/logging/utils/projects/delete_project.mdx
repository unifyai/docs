---
title: 'delete_project'
---

```python
def delete_project(
    name: str,
    *,
    missing_ok: bool = True,
    api_key: Optional[str] = None,
) -> str:
```

<p align="right">[source code](https://github.com/unifyai/unify/tree/157ca59a0fa10cd80a4424822fce7919d8958f4b/unify/logging/utils/projects.py#L100)</p>

Deletes a project from your account.

**Arguments**:

- `name` - Name of the project to delete.
- `missing_ok` - If True (default), silently succeeds when the project does not exist.
- `api_key` - If specified, unify API key to be used. Defaults to the value in the



**Returns**:

Whether the project was successfully deleted.