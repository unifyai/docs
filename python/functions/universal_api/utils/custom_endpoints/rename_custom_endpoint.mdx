---
title: 'rename_custom_endpoint'
---

```python
def rename_custom_endpoint(
    name: str,
    new_name: str,
    *,
    api_key: Optional[str] = None,
) -> Dict[str, Any]:
```

<p align="right">[source code](https://github.com/unifyai/unify/tree/ca3255d6b7040c77eabbe4ca6f18b9dae949463f/unify/universal_api/utils/custom_endpoints.py#L100)</p>

Rename a custom endpoint.

**Arguments**:

- `name` - Name of the custom endpoint to be updated.
- `new_name` - New name for the custom endpoint.
- `api_key` - If specified, unify API key to be used. Defaults



**Returns**:

A dictionary containing the response information.



**Raises**:

- `requests.HTTPError`: If the API request fails.