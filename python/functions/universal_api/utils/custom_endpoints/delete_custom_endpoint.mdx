---
title: 'delete_custom_endpoint'
---

```python
def delete_custom_endpoint(
    name: str,
    *,
    api_key: Optional[str] = None,
) -> Dict[str, str]:
```

<p align="right">[source code](https://github.com/unifyai/unify/tree/d5923c1dc4a051f511be5f16587d4a50123cbab9/unify/universal_api/utils/custom_endpoints.py#L64)</p>

Delete a custom endpoint.

**Arguments**:

- `name` - Name of the custom endpoint to delete.
- `api_key` - If specified, unify API key to be used. Defaults



**Returns**:

A dictionary containing the response message.



**Raises**:

- `requests.HTTPError`: If the API request fails.