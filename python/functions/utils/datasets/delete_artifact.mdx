---
title: 'delete_artifact'
---

```python
def delete_artifact(
    name: str,
    key: str,
    api_key: Optional[str] = None,
) -> Union[List[Any], None]:
```

<p align="right">[source code](https://github.com/unifyai/unify/tree/e6c7d27bf82ff26f660d6b58b5f7e41d26b026f9/unify/utils/datasets.py#L348)</p>

Downloads a dataset from the platform.

**Arguments**:

- `name` - Name of the dataset to delete an artifact from.
- `key` - The key of the artifact to delete
- `api_key` - If specified, unify API key to be used. Defaults to the value in the