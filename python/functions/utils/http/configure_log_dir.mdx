---
title: 'configure_log_dir'
---

```python
def configure_log_dir(log_dir: Optional[str] = None) -> Optional[Path]:
```

<p align="right">[source code](https://github.com/unifyai/unify/tree/e54686fb35c062d67226ee1d028a7a3efcbb8020/unify/utils/http.py#L48)</p>

Configure or reconfigure the log directory for file-based logging.Call this after setting UNIFY_LOG_DIR if the env var was set
after this module was imported.

**Arguments**:

- `log_dir` - Explicit log directory path. If None, reads from



**Returns**:

The configured log directory Path, or None if disabled.