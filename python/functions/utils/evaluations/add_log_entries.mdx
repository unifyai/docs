---
title: 'add_log_entries'
---

```python
def add_log_entries(
    id: Optional[int] = None,
    api_key: Optional[str] = None,
    **kwargs,
) -> Dict[str, str]:
```

<p align="right">[source code](https://github.com/unifyai/unify/tree/c4f07243fe885d2143baa0cb3d147c8acc858e1e/unify/utils/evaluations.py#L370)</p>

Returns the data (id and values) by querying the data based on their values.

**Arguments**:

- `id` - The log id to update with extra data. Looks for the current active log if no id is provided.
- `api_key` - If specified, unify API key to be used. Defaults to the value in the
- `kwargs` - The data to log into the console.



**Returns**:

A message indicating whether the log was successfully updated.