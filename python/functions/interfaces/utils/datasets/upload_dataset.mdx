---
title: 'upload_dataset'
---

```python
def upload_dataset(
    name: str,
    content: list,
    *,
    api_key: Optional[str] = None,
) -> Dict[str, str]:
```

<p align="right">[source code](https://github.com/unifyai/unify/tree/fe567e0be3a401d4435b8a88dbb6f0014ab8e18c/unify/interfaces/utils/datasets.py#L17)</p>

Uploads a dataset to the platform

**Arguments**:

- `name` - The name of the dataset
- `content` - List of entries to upload
- `api_key` - If specified, unify API key to be used. Defaults to the value in the