---
title: 'Rename Default Prompt'
api: 'POST /v0/default_prompt/rename'
---
Renames a default prompt from `name` to `new_name` in your account.

#### Authorizations

<ParamField header="Authorization" type="string" required="true">
  Bearer authentication header of the form `Bearer <token>`, where `<token>` is your auth token.
</ParamField>

#### Query Parameters

<ParamField query="name" type="string" required="true">
Name of the default prompt to rename.
</ParamField>

<ParamField query="new_name" type="string" required="true">
New name for the default prompt.
</ParamField>

<RequestExample>

```bash cURL
curl --request POST \
  --url 'https://api.unify.ai/v0/default_prompt/rename?name=default_prompt_1&new_name=default_prompt_2' \
  --header "Authorization: Bearer $UNIFY_KEY"
```

```python Python
import requests

url = "https://api.unify.ai/v0/default_prompt/rename?name=default_prompt_1&new_name=default_prompt_2"

headers = {"Authorization": "Bearer <token>"}

response = requests.request("POST", url, headers=headers)

print(response.text)
```

</RequestExample>
<ResponseExample>

```json 200
{
    "info": "Default Prompt renamed successfully!"
}
```

```json 422
{
    "detail": [
        {
            "loc": [
                "string"
            ],
            "msg": "string",
            "type": "string"
        }
    ]
}
```

</ResponseExample>
