---
title: "Update billing profile"
api: 'PATCH /v0/billing/billing-profile'
---
Update the billing profile for the current workspace. Context (personal vs org) is derived from the API key.

#### Authorizations

<ParamField header="Authorization" type="string" required="true">
  Bearer authentication header of the form `Bearer <token>`, where `<token>` is your auth token.
</ParamField>

#### Body

<ParamField body="billing_email" type="string | null" >

</ParamField>

<ParamField body="name" type="string | null" >

</ParamField>

<ParamField body="tax_id" type="string | null" >

</ParamField>

<ParamField body="tax_id_type" type="string | null" >

</ParamField>

<ParamField body="billing_address" type="object | null" >

</ParamField>

<RequestExample>

```bash cURL
curl --request PATCH \
  --url 'https://api.unify.ai/v0/billing/billing-profile' \
  --header "Authorization: Bearer $UNIFY_KEY" \
  --header 'Content-Type: application/json' \
  --data '{}'
```

```python Python
import requests

url = "https://api.unify.ai/v0/billing/billing-profile"

headers = {"Authorization": "Bearer <token>"}

json_input = {}

response = requests.request("PATCH", url, json=json_input, headers=headers)

print(response.text)
```

</RequestExample>
<ResponseExample>

```json 200
{
    "billing_email": "string | null",
    "name": "string | null",
    "tax_id": "string | null",
    "tax_id_type": "string | null",
    "billing_address": "object",
    "billing_setup_complete": "boolean",
    "is_business": "boolean"
}
```

```json 422
{
    "detail": [
        {
            "loc": [
                "string"
            ],
            "msg": "string",
            "type": "string"
        }
    ]
}
```

</ResponseExample>
