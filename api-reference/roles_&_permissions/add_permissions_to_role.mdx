---
title: "Add Permissions To Role"
api: 'POST /v0/organizations/{organization_id}/roles/{role_id}/permissions'
---
Add permissions to a custom role.

Only organization owners can modify role permissions.
Cannot modify system roles.

:param request_fastapi: FastAPI request object.
:param organization_id: Organization ID.
:param role_id: Role ID.
:param permission_data: Permissions to add.
:param session: Database session.
:return: Updated role.

#### Authorizations

<ParamField header="Authorization" type="string" required="true">
  Bearer authentication header of the form `Bearer <token>`, where `<token>` is your auth token.
</ParamField>

#### Path Parameters

<ParamField query="organization_id" type="integer" required="true">

</ParamField>

<ParamField query="role_id" type="integer" required="true">

</ParamField>

#### Body

<ParamField body="permission_ids" type="[integer]" required="true">

</ParamField>

<RequestExample>

```bash cURL
curl --request POST \
  --url 'https://api.unify.ai/v0/organizations/<organization_id>/roles/<role_id>/permissions' \
  --header "Authorization: Bearer $UNIFY_KEY"
  --header 'Content-Type: application/json' \
  --data '{}'
```

```python Python
import requests

url = "https://api.unify.ai/v0/organizations/<organization_id>/roles/<role_id>/permissions"

headers = {"Authorization": "Bearer <token>"}

json_input = {}

response = requests.request("POST", url, json=json_input, headers=headers)

print(response.text)
```

</RequestExample>
<ResponseExample>

```json 200
{
    "id": "integer",
    "name": "string",
    "description": "string | null",
    "organization_id": "integer | null",
    "is_system_role": "boolean",
    "created_at": "string",
    "permissions": "[Any]"
}
```

```json 422
{
    "detail": [
        {
            "loc": [
                "string"
            ],
            "msg": "string",
            "type": "string"
        }
    ]
}
```

</ResponseExample>
