---
title: 'Add Dataset Entries'
api: 'POST /v0/datasetv2/{name}/entries'
---
Add multiple entries to a dataset.

#### Authorizations

<ParamField header="Authorization" type="string" required="true">
  Bearer authentication header of the form `Bearer <token>`, where `<token>` is your auth token.
</ParamField>

#### Path Parameters

<ParamField query="name" type="string" required="true">
Dataset name (can include forward slashes)
</ParamField>

#### Body

<ParamField body="entries" type="[Any]" required="true">
List of entries to add
</ParamField>

<RequestExample>

```bash cURL
curl --request POST \
  --url 'https://api.unify.ai/v0/datasetv2/my_dataset/entries' \
  --header "Authorization: Bearer $UNIFY_KEY"
  --header 'Content-Type: application/json' \
  --data '{
    "entries": [
        "id_1",
        "id_2",
        "id_3"
    ]
}'
```

```python Python
import requests

url = "https://api.unify.ai/v0/datasetv2/my_dataset/entries"

headers = {"Authorization": "Bearer <token>"}

json_input = {"entries": ["id_1", "id_2", "id_3"]}

response = requests.request("POST", url, json=json_input, headers=headers)

print(response.text)
```

</RequestExample>
<ResponseExample>

```json 201
{
    "added": [
        "id_1",
        "id_2",
        "id_3"
    ],
    "already_present": [
        "id_4",
        "id_5"
    ]
}
```

```json 404
"Dataset not found."
```

```json 422
{
    "detail": [
        {
            "loc": [
                "string"
            ],
            "msg": "string",
            "type": "string"
        }
    ]
}
```

</ResponseExample>
