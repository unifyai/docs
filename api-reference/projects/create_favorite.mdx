---
title: 'Create Favorite'
api: 'POST /v0/project/favorites'
---
Creates a new favorite project for the user.

Each favorite must include a project name, icon, and position.

#### Authorizations

<ParamField header="Authorization" type="string" required="true">
  Bearer authentication header of the form `Bearer <token>`, where `<token>` is your auth token.
</ParamField>

#### Body

<ParamField body="project" type="string" required="true">
The name of the project to favorite
</ParamField>

<ParamField body="icon" type="string" required="true">
Icon identifier for the favorite project
</ParamField>

<ParamField body="position" type="integer" required="true">
Position of the project in the favorites list
</ParamField>

<RequestExample>

```bash cURL
curl --request POST \
  --url 'https://api.unify.ai/v0/project/favorites' \
  --header "Authorization: Bearer $UNIFY_KEY" \
  --header 'Content-Type: application/json' \
  --data '{}'
```

```python Python
import requests

url = "https://api.unify.ai/v0/project/favorites"

headers = {"Authorization": "Bearer <token>"}

json_input = {}

response = requests.request("POST", url, json=json_input, headers=headers)

print(response.text)
```

</RequestExample>
<ResponseExample>

```json 201
{
    "id": 1,
    "project": "my-project",
    "icon": "star",
    "position": 0
}
```

```json 400
{
    "detail": "Project is already in favorites"
}
```

```json 404
{
    "detail": "Project 'unknown-project' not found"
}
```

```json 422
{
    "detail": [
        {
            "loc": [
                "string"
            ],
            "msg": "string",
            "type": "string"
        }
    ]
}
```

</ResponseExample>
